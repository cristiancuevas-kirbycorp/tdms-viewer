<Window x:Class="TdmsViewer.ScaleEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Y-Scale" SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <StackPanel Margin="16" MinWidth="280">
        <TextBlock Text="Scale name" FontWeight="SemiBold" Margin="0,0,0,3" />
        <TextBox x:Name="NameBox" Margin="0,0,0,12" />

        <CheckBox x:Name="AutoBox" Content="Auto scale" Margin="0,0,0,10"
                  Checked="Auto_Changed" Unchecked="Auto_Changed" />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Min" FontWeight="SemiBold" Margin="0,0,0,3" />
                <TextBox x:Name="MinBox" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Max" FontWeight="SemiBold" Margin="0,0,0,3" />
                <TextBox x:Name="MaxBox" />
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="14,5" Margin="0,0,8,0" />
            <Button Content="OK" IsDefault="True" Padding="14,5" Click="Ok_Click" />
        </StackPanel>
    </StackPanel>
</Window>
